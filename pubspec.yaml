name: dolpin
description: "K-pop concert item C2C rental marketplace"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0

  # Supabase
  supabase_flutter: ^2.12.4

  # Routing
  go_router: ^14.8.1

  # Image
  image_picker: ^1.2.3
  cached_network_image: ^3.4.1

  # UI
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0
  intl: ^0.20.3

  # Payment
  iamport_flutter: ^0.10.20
  http: ^1.2.2

  # Notifications
  flutter_local_notifications: ^18.0.1

  # Crash Reporting
  sentry_flutter: ^8.12.0

  # Utility
  # `characters` is transitively available via flutter, but the
  # `depend_on_referenced_packages` lint requires it to be declared
  # explicitly whenever we import `package:characters/characters.dart`
  # (see Validators.sanitizeSearch which uses grapheme-safe .take()).
  characters: ^1.3.0
  geolocator: ^13.0.2
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  shared_preferences: ^2.5.5
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  envied: ^1.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  build_runner: ^2.15.1
  riverpod_generator: ^4.0.0+1
  freezed: ^3.2.3
  json_serializable: ^6.11.2
  envied_generator: ^1.3.8
  custom_lint: ^0.8.1
  riverpod_lint: ^3.1.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
